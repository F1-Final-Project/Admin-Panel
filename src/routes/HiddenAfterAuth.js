import React from 'react'import { Redirect, Route } from 'react-router-dom'const HiddenAfterAuth = ({ component: Component, ...rest }) => (	<Route		{...rest}		render={props =>			sessionStorage.getItem('token') ? (				<Redirect					to={{						pathname: '/adminPanel',						state: { from: props.location },					}}				/>			) : (				<Component {...props} />			)		}	/>)export default HiddenAfterAuth