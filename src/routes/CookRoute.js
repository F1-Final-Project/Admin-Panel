import React from 'react'import { Redirect, Route } from 'react-router-dom'const CookRoute = ({ component: Component, ...rest }) => (    <Route        {...rest}        render={props =>            sessionStorage.getItem('permission') === 'admin' ||            sessionStorage.getItem('permission') === 'cook' ? (                <Component {...props} />            ) : (                <Redirect                    to={{                        pathname: '/login',                        state: { from: props.location },                    }}                />            )        }    />)export default CookRoute