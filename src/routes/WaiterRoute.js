import React from 'react'import { Redirect, Route } from 'react-router-dom'const WaiterRoute = ({ component: Component, ...rest }) => (    <Route        {...rest}        render={props =>            sessionStorage.getItem('permission') === 'admin' ||            sessionStorage.getItem('permission') === 'waiter' ? (                <Component {...props} />            ) : (                <Redirect                    to={{                        pathname: '/login',                        state: { from: props.location },                    }}                />            )        }    />)export default WaiterRoute