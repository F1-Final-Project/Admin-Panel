import {makeStyles} from '@material-ui/core'

export default makeStyles(theme => ({
    error:{
        backgroundColor: '#CC0000',
    },
    icon:{
        display: 'flex',
        alignItems: 'center',
    },
    close:{
        fontSize: 20,
    }
}))